# Optional: enable real OpenAI calls (if unset, mock mode is used)
# OPENAI_API_KEY=...
OPENAI_MODEL=gpt-5
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

CHUNK_SIZE=800
CHUNK_OVERLAP=120

# Redis (required for real vector storage; use Redis Stack for RediSearch)
REDIS_URL=redis://localhost:6379
REDIS_INDEX=rag:chunks
REDIS_PASSWORD=
