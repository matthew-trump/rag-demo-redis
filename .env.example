# Optional: enable real OpenAI calls (if unset, mock mode is used)
# OPENAI_API_KEY=...
OPENAI_MODEL=gpt-5
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

CHUNK_SIZE=800
CHUNK_OVERLAP=120

# Milvus (required for real vector storage)
MILVUS_URI=http://localhost:19530
# MILVUS_TOKEN=...          # optional if auth enabled
MILVUS_COLLECTION=rag_demo
